/*
    The API files is going be serving general purpose API endpoints (most likely RESTful) that are not related to any specific domain model.
    However, the express server is most likely going to be serving the API and handles scaling
    socket io is also potentially going to be used for real time communication
*/

/*
    API endpoints:
    - /game
    - /game/:id (get, post, put, delete)
*/
